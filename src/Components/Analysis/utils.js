export const getColor = ({theme})=>{
    console.log(theme)
    switch(theme) {
        case 'blue':
          return "#8575ff79"
        case 'green':
          return "#00b30079"
        default:
          // code block
      }
}